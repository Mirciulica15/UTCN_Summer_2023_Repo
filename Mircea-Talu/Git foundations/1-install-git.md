# Install git on Ubuntu

* check if git is installed
* **dpkg -l | grep git**
* **ii** means correct installation
* or just run **git**
* if it's not installed:
* **apt install -y git**
* we use the **apt** command
* check with **git --version**
